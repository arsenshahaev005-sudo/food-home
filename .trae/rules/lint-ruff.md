# lint_ruff.md


# 🤖 AI Linting Rules (Frontend + Backend)

Этот документ обязателен для **всех нейросетей / AI-агентов**, которые вносят изменения в репозиторий.

Любой код **СЧИТАЕТСЯ НЕКОРРЕКТНЫМ**, если он не проходит линт.

---

## 0. Структура проекта

```
food-home/
├── frontend/   # Next.js (ESLint)
└── backend/    # Django / Python (Ruff)
```

ИИ **ОБЯЗАН** определить, в какой части проекта он работает.

---

## 1. Обязательность линтинга

Любое изменение кода **ОБЯЗАТЕЛЬНО** сопровождается линтингом.

| Часть проекта | Инструмент                  | Команда                              |
| ------------- | --------------------------- | ------------------------------------ |
| Frontend      | ESLint + eslint-config-next | `npm run lint` (из `frontend/`)      |
| Backend       | Ruff                        | `ruff check . --fix` (из `backend/`) |

❌ Запрещено:

* игнорировать ошибки линтера
* говорить «это не критично»
* предлагать код без чистого линта

---

## 2. Порядок работы AI (СТРОГО)

AI **ВСЕГДА** действует в следующем порядке:

1. Определить: frontend или backend
2. Внести изменения в код
3. Запустить соответствующий линт
4. Исправить **ВСЕ** ошибки и предупреждения
5. Только после этого считать задачу выполненной

---

## 3. Frontend правила (Next.js)

### 3.1 Инструменты

* Используется **ТОЛЬКО** ESLint
* Конфигурация: `eslint-config-next`
* Линт запускается **ТОЛЬКО из папки `frontend/`**

❌ Запрещено:

* запускать ESLint из корня проекта
* использовать ESLint 9
* использовать `@rushstack/eslint-patch`
* смешивать разные версии ESLint

---

### 3.2 Стиль кода

* Следовать правилам ESLint и Prettier
* Не отключать правила без причины
* Не использовать устаревшие API React / Next.js

---

### 3.3 Обязательный отчёт AI (frontend)

После любого изменения AI **ОБЯЗАН** указать:

```
✔ frontend lint (ESLint) — без ошибок
```

---

## 4. Backend правила (Python / Django)

### 4.1 Инструменты

* Используется **ТОЛЬКО Ruff**
* Ruff заменяет: flake8, isort, black
* Линт запускается **ТОЛЬКО из папки `backend/`**

---

### 4.2 Импорты

* Импорты сортируются автоматически Ruff
* Ручная сортировка запрещена

❌ Запрещено оставлять ошибки:

* `I001`, `I002`

---

### 4.3 Ошибки и предупреждения

* Любое предупреждение Ruff = ошибка
* `# noqa` допускается **ТОЛЬКО** с пояснением

---

### 4.4 Обязательный отчёт AI (backend)

После любого изменения AI **ОБЯЗАН** указать:

```
✔ backend lint (ruff) — без ошибок
```

---

## 5. Запрещённые действия (ОБЩИЕ)

AI **НЕ ИМЕЕТ ПРАВА**:

* игнорировать линтер «ради скорости»
* менять конфигурации ESLint / Ruff без запроса
* добавлять `disable` без комментария
* предлагать код, который не проходит линт

---

## 6. Финальный отчёт AI (обязателен)

Каждый ответ с кодом **ЗАКАНЧИВАЕТСЯ** отчётом:

```
✔ frontend lint — OK
✔ backend lint — OK
```

(указывается только соответствующая часть проекта)

---

## 7. Краткое правило (если читается только одно)

> **Код без чистого линта = неправильный код.**

---

📌 Этот файл обязателен к соблюдению всеми нейросетями, агентами и автоматическими инструментами.
